export const environment = {
  production: false,
  // Temporary bridge while modules are migrated away from the Spring backend.
  proxyPath: '/api/proxy',
  supabase: {
    url: '',
    publishableKey: '',
  },
};
